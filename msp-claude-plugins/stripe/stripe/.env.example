# Stripe MCP — connects to Stripe's first-party hosted server (mcp.stripe.com).
# OAuth 2.1 + PKCE PUBLIC CLIENT (token_endpoint_auth_methods=['none']).
# Set the canonical client_id provisioned via one-time POST to
# https://access.stripe.com/mcp/oauth2/register; see WYRE Gateway docs.
STRIPE_CLIENT_ID=""
