# Slack MCP — connects to Slack's first-party hosted server (mcp.slack.com/mcp).
# OAuth 2.1 + PKCE CONFIDENTIAL CLIENT (client_secret_post; BYOC operator app).
# Slack does NOT support RFC 7591 DCR — operators must register their own
# Slack app via api.slack.com/apps and provide its client_id + client_secret.
SLACK_CLIENT_ID=""
SLACK_CLIENT_SECRET=""
