# @wyre-ai packages live on GitHub Packages. NODE_AUTH_TOKEN is set only
# when publishing (Actions GITHUB_TOKEN or a local write:packages PAT).
@wyre-ai:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
